# SKILL.md 技能化重构 Implementation Plan（待审批）

> **状态**：✅ **已批准（2026-08-14）**——保留 SKILL.md 技能化重构；批准前不做任何进一步修改。
> （本计划为补办审批：修改曾先行应用，违反"先计划后执行"规则。）

**Goal:** 让 SKILL.md 符合"一个 skill 的特点"——触发驱动、用户语言、精简、委托 CLI，而非引擎能力罗列。这是对用户原则"一切修改方向应符合 skill 特点"的直接响应。

**改动内容（已应用，待审批）：**
1. **description 重写**：保留 8 个英文触发域关键词 + 8 个中文触发词（契约测试锁定），改为触发导向（~120 词），去掉能力 ID。
2. **「何时使用」从能力罗列 → 用户语言触发表**：10 行"用户问 → 能力 → 输出"映射（含此前缺失的补缴、个人养老金税优触发）。用户说"退休后能领多少"而非"PENSION_ESTIMATION"。
3. **移除引擎内部细节**：省本级层/信封结构等过深说明压缩为 agent 所需的一行。
4. **修复冗余**（LOCAL_MVP 边界中的重复句）。
5. **保留**：工作流（命令式 5 步）、隐私边界、CLI 命令、输出处理（契约要求的 analysis.json/manifest.json 等）、免责声明、不应触发边界。

**验证（已执行）：** `tests/e2e/test_skill_contract.py` 17/17 通过（含全部契约约束：触发词、无内联政策数字、隐私、CLI、输出处理、边界、免责）。

**文件：** 仅 `SKILL.md`（127 行 → ~120 行）。

**审批选项：**
- **批准**：保留本修改，后续按新流程执行。
- **不批准**：回滚 `SKILL.md` 至重写前版本（含旧的能力罗列式"何时使用"）。

**后续治理流程（自本计划起生效）：**
1. 任何修改先写计划到 `docs/plans/`（含目标、改动文件、验证方式）。
2. 提交用户审批；批准后方可执行。
3. 执行中每步验证；完成记录到执行日志。

# v0.1.2 工程治理 Pre-release

> 状态：GitHub 工程 Pre-release（`--prerelease`）。本发布不授予独立生产审批，详见 `docs/release-governance.md` 的「工程预发布与非生产审批语义」。

## 版本职责分离

- 包发行版本 `PACKAGE_VERSION = 0.1.2`，与 `pyproject.toml` 一致。
- 引擎语义版本 `ENGINE_SEMANTICS_VERSION = 0.1.1` 保持不变：本次发布不改变计算、资格、能力状态、规范化或结构化输出语义。
- 北京 golden `run_id` 保持不变：`run-95e2c71f61a9b8510cc4097e9c930d53afb36a4892be154802ac96c4687731e9`。

## 本版内容

- #6：引入 Ruff 与增量 mypy 质量门禁（`quality` extra 固定工具版本，本地与 CI 同一配置）。
- #7：公开安全与贡献治理（`SECURITY.md`、三类 Issue 模板、PR 模板、README 治理链接）。
- 工程发行自动化：标签触发的 Release 工作流，包含质量门禁、测试、依赖漏洞、许可证清单、密钥基线、SBOM、构建校验和、安装/升级/回滚/重放演练、证据上传与构建证明，以及 Pre-release 发布。
- 发行安装形态修复：schemas 与 `policy-data` 随 wheel 分发（`share/china-pension-strategy`），数据根解析支持 `CHINA_PENSION_DATA_ROOT` 环境变量；wheel 安装后可直接运行。`v0.1.1` 仅有源码/editable 可运行形态（发行前从未安装运行），回滚演练按该形态验证。
- 完整本地门禁：657 项自动化测试、设计文档与契约、架构审计、Ruff 与 mypy 全部通过；CI 在 Python 3.12、3.13、3.14 上运行。

## 未包含

- 本发布不授予独立生产审批；`MVP_REVIEWED` 包仍受运行时状态门禁约束。
- 广州医疗费率规则将于 2026-12-31 到期（#8 保持开放）：到期报告仍可能对当前规则返回非零。

## 回滚目标

- 包版本 `v0.1.1`（提交 `034bdf6`）。

## 证据清单

- `release-evidence/`：测试摘要、许可证清单、密钥扫描结果、SBOM、依赖锁、校验和、安装/升级/回滚/重放演练记录。
- `dist/`：wheel 与 sdist，附构建证明（attestation）。